{
  "name": "budgi",
  "version": "0.0.1",
  "description": "Budgi supabase",
  "scripts": {
    "start": "npx supabase start",
    "stop": "npx supabase stop --no-backup",
    "migrations-pull": "npx supabase db pull",
    "migrations-apply": "npx supabase migrations up"
  },
  "devDependencies": {
    "supabase": "^1.115.5"
  }
}
